# Write the python program to check the two string is anagram or not .

def check(a,ch) :
  if sorted(a) == sorted(ch) : print("Anagram")
  else :  print("Not Anagram")
  
a = input("Enter the String 1   ")
ch = input("Enter the String 2  ")
check(a,ch)
